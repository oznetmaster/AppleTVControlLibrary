<Window x:Class="AppleTvControlLibrary.Remote.Wpf.Views.TextInputDialog"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Text Input" Height="160" Width="360"
		WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
		Closing="OnClosing">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" x:Name="PromptText" Text="The Apple TV is requesting text input:" TextWrapping="Wrap" Margin="0,0,0,8" />
		<TextBox Grid.Row="1" x:Name="InputTextBox" TextChanged="OnInputTextChanged" />

		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
			<Button Content="Done" IsDefault="True" Click="OnDoneClick" Padding="12,4" Margin="0,0,8,0" />
			<Button Content="Cancel" Click="OnCancelClick" Padding="12,4" />
		</StackPanel>
	</Grid>
</Window>
