# Discovery API Reference

The Discovery API reference documents the separately usable mDNS/DNS-SD discovery library for
Companion Link (`_companion-link._tcp`) and MRP (`_mediaremotetv._tcp`) services.

Use the navigation tree to browse namespaces and public types.