# MRP API Reference

The MRP API reference documents `AppleTvControlLibrary.Mrp`: pairing/session setup, AirPlay
tunneling, player/queue state tracking, and the high-level `MrpRemoteControl` command surface.

Use the navigation tree to browse namespaces and public types. This reference is generated
directly from the library's XML documentation comments, each of which cites the pyatv source it
was ported from where applicable.
