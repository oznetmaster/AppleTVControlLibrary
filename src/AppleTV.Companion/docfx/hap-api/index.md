# Shared HAP API Reference

The shared HAP API reference documents AppleTv.Hap: the HomeKit Accessory Protocol primitives used by both the Companion Link and MRP protocol libraries (SRP, ChaCha20-Poly1305, OPACK, TLV8).

Use the navigation tree to browse namespaces and public types.
